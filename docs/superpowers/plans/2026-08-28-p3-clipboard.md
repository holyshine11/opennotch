# OpenNotch P3 — 클립보드 히스토리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 텍스트·URL·이미지·파일 목록을 최대 N개(설정) 보관하는 클립보드 히스토리. 칩 클릭 = 클립보드에 복사(사용자가 ⌘V), 검색, 삭제, 모두 지우기. 비밀번호 관리자 항목 제외. macOS 15.4+ 클립보드 프라이버시 알림에 대비한 게이트.

**Architecture:** `ClipboardStore`(@Observable, JSON + PNG 파일)가 데이터를 소유하고, `ClipboardMonitor`가 주입된 `NSPasteboard`의 `changeCount`를 0.75초 간격으로 폴링해 변경 시 분류·저장한다(프라이버시 게이트·제외 타입·자기 마커). `ClipboardView`는 칩 5개 + ▸/검색 시 위쪽 두 칸을 덮는 overlay 목록. `AppDelegate`가 `paneProvider.clipboard`에 연결하고 설정 변화(사용 여부·상한)와 화면 잠금을 반영한다. 유휴 타이머 리셋은 루트 뷰 한 곳에서 처리한다(P2 최종 리뷰 R3).

**Tech Stack:** Swift 6.3(언어 모드 6), SwiftUI + AppKit, Swift Testing. 외부 의존성 0.

**Spec:** `docs/superpowers/specs/2026-08-28-opennotch-design.md` v1.3 — §3.3(클립보드 행·overlay), §3.4(클립보드), §4.3(계약), §4.7(클립보드), §4.9(설정 키)

## Global Constraints

- 최소 macOS **14.0**, Swift 언어 모드 **6**, 외부 의존성 **0개**. 사설 API·Accessibility·CGEvent 붙여넣기 시뮬레이션 **금지**(붙여넣기 = 클립보드에 쓰기만).
- 제외 타입: `org.nspasteboard.ConcealedType`, `org.nspasteboard.TransientType`, `org.nspasteboard.AutoGeneratedType`, 자기 마커 `org.opennotch.source`. 로그에 클립보드 내용·파일 경로 금지.
- 프라이버시 게이트(§4.7): `if #available(macOS 15.4, *)` — general 페이스트보드의 `accessBehavior == .alwaysAllow`일 때만 타이머가 내용을 읽는다. `.default`/`.ask`/`.alwaysDeny`면 읽지 않고 `pasteboardBlocked = true`. `enableWithUserAction()`(사용자 버튼)은 `.default`에서 1회 읽는다. macOS 14는 항상 읽기. `changeCount`·`types()`는 알림 대상이 아니다(실측).
- 파일 URL 항목은 **경로 표시·클립보드 재복사만**(재시작 후 접근 보장 없음, bookmark 없음 — P2 리뷰 R4).
- 이미지: PNG로 저장, 10MB 초과 폐기. 저장 위치 `Application Support/OpenNotch/clipboard.json` + `clipboard/<id>.png`.
- 접힌 상태 alpha-0 규칙·P1 인터페이스 유지. 문자열 영어. quality-gate(D10): 상수는 `Constants`에 타입 명시, 소스에 `https://` 금지(URL 판별은 `URL(string:)?.scheme`으로).
- 커밋: 태스크마다, 한국어 메시지 + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, `git -c user.name="holyshine11" -c user.email="anodpark@gmail.com" commit`. `OpenNotch.xcodeproj` 커밋 금지.
- 명령(루트 `/Users/Dev/Notch_app`): `xcodegen generate` · `xcodebuild -project OpenNotch.xcodeproj -scheme OpenNotch -destination 'platform=macOS' test 2>&1 | tail -40; echo "rc=${PIPESTATUS[0]}"`(rc=0 + 테스트 목록으로 판정).
- 시작 브랜치: `feat/p3-clipboard`(P2 브랜치 `feat/p2-shelf-airdrop` 위 스택, P2 병합 후 main 기준).

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `Sources/Clipboard/ClipItem.swift` | Codable 값 타입 + `ClipKind` |
| `Sources/Clipboard/ClipboardStore.swift` | 저장/로드, 상한, 연속 중복 병합, 검색, 삭제, 클립보드에 쓰기(마커 포함) |
| `Sources/Clipboard/ClipboardMonitor.swift` | 폴링(`poll()` 분리), 프라이버시 게이트, 제외, 분류, 잠금 일시정지 |
| `Sources/Clipboard/ClipboardView.swift` | 칩 행, overlay 목록, 검색, 우클릭 삭제, 게이트 안내 |
| `Sources/NotchWindow/NotchRootView.swift` (수정) | 루트 유휴 리셋(`simultaneousGesture`) |
| `Sources/App/Constants.swift`, `Preferences.swift`(수정) | 상수·알림 이름 |
| `Sources/App/SettingsView.swift` (수정) | "Clear history" 버튼 |
| `Sources/App/AppDelegate.swift` (수정) | 스토어·모니터 생성, 페인 연결, 설정·잠금 반영 |
| `Tests/ClipboardStoreTests.swift`, `Tests/ClipboardMonitorTests.swift` | 실제 임시 디렉터리·이름 있는 페이스트보드로 검증 |

---

### Task 1: ClipItem + ClipboardStore

**Files:**
- Create: `Sources/Clipboard/ClipItem.swift`, `Sources/Clipboard/ClipboardStore.swift`
- Modify: `Sources/App/Constants.swift`
- Test: `Tests/ClipboardStoreTests.swift`

**Interfaces:**
- Produces:
  - `enum ClipKind: String, Codable { text, url, image, files }`
  - `struct ClipItem: Codable, Identifiable, Equatable { let id: UUID; var createdAt: Date; let kind: ClipKind; let text: String?; let imageFile: String?; let filePaths: [String]?; let byteCount: Int; var title: String }`
  - `@MainActor @Observable final class ClipboardStore { init(directory: URL? = nil); private(set) var items: [ClipItem]; var limit: Int; var pasteboardBlocked: Bool; func insert(_ item: ClipItem, imagePNG: Data?); func remove(id:); func removeAll(); func search(_ query: String) -> [ClipItem]; func imageURL(for id:) -> URL?; func write(_ item: ClipItem, to pasteboard: NSPasteboard) }`
  - `Constants.clipboardStoreFileName = "clipboard.json"`, `clipboardImageDirectoryName = "clipboard"`, `clipboardMaxImageBytes: Int = 10_000_000`, `clipboardPollInterval: TimeInterval = 0.75`, `clipboardChipCount: Int = 5`, `clipboardListHeight: CGFloat = 140`, `clipboardTitleMaxLength: Int = 60`
  - `NSPasteboard.PasteboardType.openNotchSource = .init("org.opennotch.source")`

- [ ] **Step 1: 실패하는 테스트**

`Tests/ClipboardStoreTests.swift`:
```swift
import AppKit
import Foundation
import Testing
@testable import OpenNotch

@MainActor
@Suite struct ClipboardStoreTests {
    private func makeDir() throws -> URL {
        let dir = FileManager.default.temporaryDirectory.appendingPathComponent("clip-tests-\(UUID().uuidString)", isDirectory: true)
        try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
        return dir
    }
    private func text(_ s: String) -> ClipItem { ClipItem.text(s) }

    @Test func insertPutsNewestFirstAndPersists() throws {
        let dir = try makeDir()
        let store = ClipboardStore(directory: dir)
        store.insert(text("one"), imagePNG: nil)
        store.insert(text("two"), imagePNG: nil)
        #expect(store.items.map(\.text) == ["two", "one"])
        #expect(ClipboardStore(directory: dir).items.map(\.text) == ["two", "one"])
    }

    @Test func consecutiveDuplicateTextIsMergedNotDuplicated() throws {
        let store = ClipboardStore(directory: try makeDir())
        store.insert(text("same"), imagePNG: nil)
        store.insert(text("same"), imagePNG: nil)
        #expect(store.items.count == 1)
    }

    @Test func limitEvictsOldestAndDeletesImageFile() throws {
        let dir = try makeDir()
        let store = ClipboardStore(directory: dir)
        store.limit = 2
        let png = Data([0x89, 0x50, 0x4E, 0x47])
        store.insert(ClipItem.image(byteCount: png.count), imagePNG: png)
        let imageID = store.items[0].id
        let imageURL = try #require(store.imageURL(for: imageID))
        #expect(FileManager.default.fileExists(atPath: imageURL.path))
        store.insert(text("a"), imagePNG: nil)
        store.insert(text("b"), imagePNG: nil)
        #expect(store.items.count == 2)
        #expect(store.items.map(\.text) == ["b", "a"])
        #expect(!FileManager.default.fileExists(atPath: imageURL.path))
    }

    @Test func removeAndRemoveAll() throws {
        let dir = try makeDir()
        let store = ClipboardStore(directory: dir)
        store.insert(text("x"), imagePNG: nil)
        store.insert(text("y"), imagePNG: nil)
        store.remove(id: store.items[0].id)
        #expect(store.items.map(\.text) == ["x"])
        store.removeAll()
        #expect(store.items.isEmpty)
        #expect(ClipboardStore(directory: dir).items.isEmpty)
    }

    @Test func searchIsCaseInsensitiveSubstring() throws {
        let store = ClipboardStore(directory: try makeDir())
        store.insert(text("Hello World"), imagePNG: nil)
        store.insert(text("other"), imagePNG: nil)
        #expect(store.search("world").map(\.text) == ["Hello World"])
        #expect(store.search("").count == 2)
    }

    @Test func titleIsTruncatedAndKindSpecific() throws {
        let long = String(repeating: "a", count: Constants.clipboardTitleMaxLength + 10)
        #expect(text(long).title.count == Constants.clipboardTitleMaxLength + 1)   // + ellipsis
        #expect(ClipItem.files(["/a/b.txt", "/c/d.png"]).title == "2 files")
        #expect(ClipItem.image(byteCount: 1).title == "Image")
        #expect(ClipItem.url("https://example.com/x").kind == .url)
    }

    @Test func writeToPasteboardIncludesMarkerAndContent() throws {
        let store = ClipboardStore(directory: try makeDir())
        let pb = NSPasteboard(name: NSPasteboard.Name("com.holyshine11.opennotch.test.write"))
        store.write(text("copied"), to: pb)
        #expect(pb.string(forType: .string) == "copied")
        #expect(pb.types?.contains(.openNotchSource) == true)
    }

    @Test func corruptedStoreStartsEmpty() throws {
        let dir = try makeDir()
        try Data("nope".utf8).write(to: dir.appendingPathComponent(Constants.clipboardStoreFileName))
        #expect(ClipboardStore(directory: dir).items.isEmpty)
    }
}
```
(테스트 안의 `"https://example.com/x"`는 테스트 파일이라 quality-gate 대상이 아니다.)

- [ ] **Step 2: 실패 확인** — 컴파일 실패.

- [ ] **Step 3: 구현**

`Sources/App/Constants.swift`에 추가:
```swift
    static let clipboardStoreFileName: String = "clipboard.json"
    static let clipboardImageDirectoryName: String = "clipboard"
    static let clipboardMaxImageBytes: Int = 10_000_000
    static let clipboardPollInterval: TimeInterval = 0.75
    static let clipboardChipCount: Int = 5
    static let clipboardListHeight: CGFloat = 140
    static let clipboardTitleMaxLength: Int = 60
```

`Sources/Clipboard/ClipItem.swift`:
```swift
import AppKit
import Foundation

enum ClipKind: String, Codable, Sendable { case text, url, image, files }

extension NSPasteboard.PasteboardType {
    /// 우리가 쓴 항목 표시 — 모니터가 다시 캡처하지 않는다.
    static let openNotchSource = NSPasteboard.PasteboardType("org.opennotch.source")
}

struct ClipItem: Codable, Identifiable, Equatable, Sendable {
    let id: UUID
    var createdAt: Date
    let kind: ClipKind
    let text: String?
    let imageFile: String?
    let filePaths: [String]?
    let byteCount: Int

    static func text(_ s: String) -> ClipItem {
        let isURL = URL(string: s).flatMap { $0.scheme != nil && $0.host != nil } ?? false
        return ClipItem(id: UUID(), createdAt: Date(), kind: isURL ? .url : .text, text: s, imageFile: nil, filePaths: nil, byteCount: s.utf8.count)
    }
    static func url(_ s: String) -> ClipItem { text(s) }
    static func image(byteCount: Int) -> ClipItem {
        let id = UUID()
        return ClipItem(id: id, createdAt: Date(), kind: .image, text: nil, imageFile: "\(id.uuidString).png", filePaths: nil, byteCount: byteCount)
    }
    static func files(_ paths: [String]) -> ClipItem {
        ClipItem(id: UUID(), createdAt: Date(), kind: .files, text: nil, imageFile: nil, filePaths: paths, byteCount: paths.reduce(0) { $0 + $1.utf8.count })
    }

    /// 칩·목록에 보이는 한 줄 제목.
    var title: String {
        switch kind {
        case .text, .url:
            let oneLine = (text ?? "").replacingOccurrences(of: "\n", with: " ")
            if oneLine.count > Constants.clipboardTitleMaxLength {
                return String(oneLine.prefix(Constants.clipboardTitleMaxLength)) + "…"
            }
            return oneLine
        case .image: return "Image"
        case .files:
            let paths = filePaths ?? []
            return paths.count == 1 ? (paths[0] as NSString).lastPathComponent : "\(paths.count) files"
        }
    }
}
```

`Sources/Clipboard/ClipboardStore.swift`:
```swift
import AppKit
import Foundation
import Observation
import os

@MainActor
@Observable
final class ClipboardStore {
    private(set) var items: [ClipItem] = []
    var limit: Int = Constants.clipboardDefaultLimit { didSet { trim(); persist() } }
    /// macOS 15.4+ 프라이버시 게이트에 막혀 내용을 읽지 못하는 상태(UI 안내용).
    var pasteboardBlocked = false

    private let storeFileURL: URL
    private let imageDirectory: URL
    private let logger = Logger(subsystem: "com.holyshine11.opennotch", category: "clipboard")

    init(directory: URL? = nil) {
        let dir = directory ?? FileManager.default
            .urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
            .appendingPathComponent("OpenNotch", isDirectory: true)
        storeFileURL = dir.appendingPathComponent(Constants.clipboardStoreFileName)
        imageDirectory = dir.appendingPathComponent(Constants.clipboardImageDirectoryName, isDirectory: true)
        try? FileManager.default.createDirectory(at: imageDirectory, withIntermediateDirectories: true)
        load()
    }

    // MARK: 조회

    func search(_ query: String) -> [ClipItem] {
        let q = query.trimmingCharacters(in: .whitespaces)
        guard !q.isEmpty else { return items }
        return items.filter { ($0.text ?? $0.title).localizedCaseInsensitiveContains(q) }
    }

    func imageURL(for id: UUID) -> URL? {
        guard let name = items.first(where: { $0.id == id })?.imageFile else { return nil }
        return imageDirectory.appendingPathComponent(name)
    }

    // MARK: 변경

    func insert(_ item: ClipItem, imagePNG: Data?) {
        if let first = items.first, first.kind == item.kind, first.kind != .image, first.text == item.text, first.filePaths == item.filePaths {
            items[0].createdAt = item.createdAt      // 연속 중복은 시각만 갱신
            persist()
            return
        }
        if let imagePNG, let name = item.imageFile {
            do { try imagePNG.write(to: imageDirectory.appendingPathComponent(name), options: .atomic) }
            catch { logger.error("image write failed: \(error.localizedDescription)"); return }
        }
        items.insert(item, at: 0)
        trim()
        persist()
    }

    func remove(id: UUID) {
        guard let index = items.firstIndex(where: { $0.id == id }) else { return }
        deleteImage(of: items.remove(at: index))
        persist()
    }

    func removeAll() {
        items.forEach(deleteImage)
        items.removeAll()
        persist()
    }

    /// 항목을 클립보드에 쓴다(마커 포함). 붙여넣기는 사용자가 ⌘V.
    func write(_ item: ClipItem, to pasteboard: NSPasteboard = .general) {
        pasteboard.clearContents()
        switch item.kind {
        case .text, .url:
            pasteboard.setString(item.text ?? "", forType: .string)
        case .image:
            if let url = imageURL(for: item.id), let data = try? Data(contentsOf: url) {
                pasteboard.setData(data, forType: .png)
            }
        case .files:
            let urls = (item.filePaths ?? []).map { URL(fileURLWithPath: $0) as NSURL }
            pasteboard.writeObjects(urls)
        }
        pasteboard.setString("1", forType: .openNotchSource)
    }

    // MARK: 내부

    private func trim() {
        while items.count > max(limit, 1) { deleteImage(of: items.removeLast()) }
    }

    private func deleteImage(of item: ClipItem) {
        guard let name = item.imageFile else { return }
        try? FileManager.default.removeItem(at: imageDirectory.appendingPathComponent(name))
    }

    private func load() {
        guard let data = try? Data(contentsOf: storeFileURL),
              let decoded = try? JSONDecoder().decode([ClipItem].self, from: data) else { items = []; return }
        items = decoded
    }

    private func persist() {
        do { try JSONEncoder().encode(items).write(to: storeFileURL, options: .atomic) }
        catch { logger.error("clipboard persist failed: \(error.localizedDescription)") }
    }
}
```

- [ ] **Step 4: 테스트 통과** — `ClipboardStoreTests` 8개, 총 41개(P2 완료 시점 33 + 8).

- [ ] **Step 5: 커밋** — `feat: ClipboardStore — JSON+PNG 영속, 상한·중복 병합·검색·클립보드 쓰기 + 테스트`

---

### Task 2: ClipboardMonitor (폴링·프라이버시 게이트·분류)

**Files:**
- Create: `Sources/Clipboard/ClipboardMonitor.swift`
- Test: `Tests/ClipboardMonitorTests.swift`

**Interfaces:**
- Produces: `@MainActor final class ClipboardMonitor { init(store: ClipboardStore, pasteboard: NSPasteboard = .general); var isEnabled: Bool; func start(); func stop(); func poll(); func enableWithUserAction(); private(set) var lastChangeCount: Int }`

- [ ] **Step 1: 실패하는 테스트**

`Tests/ClipboardMonitorTests.swift`:
```swift
import AppKit
import Foundation
import Testing
@testable import OpenNotch

@MainActor
@Suite struct ClipboardMonitorTests {
    private func make() throws -> (ClipboardMonitor, ClipboardStore, NSPasteboard) {
        let dir = FileManager.default.temporaryDirectory.appendingPathComponent("clipmon-\(UUID().uuidString)", isDirectory: true)
        try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
        let store = ClipboardStore(directory: dir)
        let pb = NSPasteboard(name: NSPasteboard.Name("com.holyshine11.opennotch.test.\(UUID().uuidString)"))
        let monitor = ClipboardMonitor(store: store, pasteboard: pb)
        monitor.isEnabled = true
        return (monitor, store, pb)
    }

    @Test func capturesPlainText() throws {
        let (m, store, pb) = try make()
        pb.clearContents(); pb.setString("hello", forType: .string)
        m.poll()
        #expect(store.items.first?.text == "hello")
        #expect(store.items.first?.kind == .text)
    }

    @Test func unchangedChangeCountDoesNothing() throws {
        let (m, store, pb) = try make()
        pb.clearContents(); pb.setString("once", forType: .string)
        m.poll(); m.poll()
        #expect(store.items.count == 1)
    }

    @Test func skipsConcealedTransientAndOwnItems() throws {
        let (m, store, pb) = try make()
        for type in ["org.nspasteboard.ConcealedType", "org.nspasteboard.TransientType", "org.nspasteboard.AutoGeneratedType"] {
            pb.clearContents()
            pb.setString("secret", forType: .string)
            pb.setString("1", forType: NSPasteboard.PasteboardType(type))
            m.poll()
        }
        pb.clearContents(); pb.setString("mine", forType: .string); pb.setString("1", forType: .openNotchSource)
        m.poll()
        #expect(store.items.isEmpty)
    }

    @Test func classifiesFilesOverImageOverText() throws {
        let (m, store, pb) = try make()
        pb.clearContents()
        pb.writeObjects([URL(fileURLWithPath: "/tmp/x.txt") as NSURL])
        pb.setString("/tmp/x.txt", forType: .string)
        m.poll()
        #expect(store.items.first?.kind == .files)
        #expect(store.items.first?.filePaths == ["/tmp/x.txt"])

        pb.clearContents()
        let png = NSImage(size: NSSize(width: 2, height: 2)).pngData()
        pb.setData(png, forType: .png)
        pb.setString("caption", forType: .string)
        m.poll()
        #expect(store.items.first?.kind == .image)
        #expect(store.imageURL(for: store.items[0].id).map { FileManager.default.fileExists(atPath: $0.path) } == true)
    }

    @Test func urlStringBecomesURLKind() throws {
        let (m, store, pb) = try make()
        pb.clearContents(); pb.setString("https://example.com/path", forType: .string)
        m.poll()
        #expect(store.items.first?.kind == .url)
    }

    @Test func disabledMonitorIgnoresChanges() throws {
        let (m, store, pb) = try make()
        m.isEnabled = false
        pb.clearContents(); pb.setString("ignored", forType: .string)
        m.poll()
        #expect(store.items.isEmpty)
    }

    @Test func oversizedImageIsDropped() throws {
        let (m, store, pb) = try make()
        pb.clearContents()
        pb.setData(Data(count: Constants.clipboardMaxImageBytes + 1), forType: .png)
        m.poll()
        #expect(store.items.isEmpty)
    }
}

private extension NSImage {
    func pngData() -> Data {
        let rep = NSBitmapImageRep(bitmapDataPlanes: nil, pixelsWide: Int(size.width), pixelsHigh: Int(size.height), bitsPerSample: 8, samplesPerPixel: 4, hasAlpha: true, isPlanar: false, colorSpaceName: .deviceRGB, bytesPerRow: 0, bitsPerPixel: 0)!
        return rep.representation(using: .png, properties: [:])!
    }
}
```

- [ ] **Step 2: 실패 확인** — 컴파일 실패.

- [ ] **Step 3: 구현**

`Sources/Clipboard/ClipboardMonitor.swift`:
```swift
import AppKit
import Foundation
import os

/// 주입된 페이스트보드의 changeCount를 폴링해 변경 시 분류·저장한다.
/// 내용 읽기는 프라이버시 게이트를 통과할 때만. 로그에 내용·경로를 남기지 않는다.
@MainActor
final class ClipboardMonitor {
    var isEnabled = false { didSet { if !isEnabled { store.pasteboardBlocked = false } } }
    private(set) var lastChangeCount: Int
    private let store: ClipboardStore
    private let pasteboard: NSPasteboard
    private var timer: Timer?
    private var lockObservers: [NSObjectProtocol] = []
    private var allowDefaultReadOnce = false
    private let logger = Logger(subsystem: "com.holyshine11.opennotch", category: "clipboard")

    private static let excludedTypes: [NSPasteboard.PasteboardType] = [
        .init("org.nspasteboard.ConcealedType"), .init("org.nspasteboard.TransientType"),
        .init("org.nspasteboard.AutoGeneratedType"), .openNotchSource,
    ]

    init(store: ClipboardStore, pasteboard: NSPasteboard = .general) {
        self.store = store
        self.pasteboard = pasteboard
        lastChangeCount = pasteboard.changeCount
    }

    func start() {
        stop()
        let t = Timer(timeInterval: Constants.clipboardPollInterval, repeats: true) { [weak self] _ in
            MainActor.assumeIsolated { self?.poll() }
        }
        t.tolerance = Constants.clipboardPollInterval / 3
        RunLoop.main.add(t, forMode: .common)
        timer = t
        let center = DistributedNotificationCenter.default()
        lockObservers = [
            center.addObserver(forName: .init("com.apple.screenIsLocked"), object: nil, queue: .main) { [weak self] _ in
                MainActor.assumeIsolated { self?.timer?.fireDate = .distantFuture }
            },
            center.addObserver(forName: .init("com.apple.screenIsUnlocked"), object: nil, queue: .main) { [weak self] _ in
                MainActor.assumeIsolated { self?.timer?.fireDate = Date() }
            },
        ]
    }

    func stop() {
        timer?.invalidate(); timer = nil
        lockObservers.forEach { DistributedNotificationCenter.default().removeObserver($0) }
        lockObservers.removeAll()
    }

    /// 사용자가 명시적으로 켤 때: `.default` 상태에서도 1회 읽어 시스템 알림이 사용자 동작에 귀속되게 한다.
    func enableWithUserAction() {
        isEnabled = true
        allowDefaultReadOnce = true
        lastChangeCount = pasteboard.changeCount - 1
        poll()
    }

    func poll() {
        guard isEnabled, pasteboard.changeCount != lastChangeCount else { return }
        lastChangeCount = pasteboard.changeCount
        guard canReadContents() else { store.pasteboardBlocked = true; return }
        store.pasteboardBlocked = false
        let types = pasteboard.types ?? []
        guard !types.contains(where: Self.excludedTypes.contains) else { return }

        if types.contains(.fileURL),
           let urls = pasteboard.readObjects(forClasses: [NSURL.self], options: [.urlReadingFileURLsOnly: true]) as? [URL], !urls.isEmpty {
            store.insert(.files(urls.map(\.path)), imagePNG: nil)
        } else if types.contains(.png) || types.contains(.tiff) {
            guard let png = pngData(from: pasteboard), png.count <= Constants.clipboardMaxImageBytes else { return }
            store.insert(.image(byteCount: png.count), imagePNG: png)
        } else if let text = pasteboard.string(forType: .string), !text.isEmpty {
            store.insert(.text(text), imagePNG: nil)
        }
    }

    // MARK: 내부

    private func canReadContents() -> Bool {
        guard pasteboard.name == .general else { return true }
        if #available(macOS 15.4, *) {
            switch pasteboard.accessBehavior {
            case .alwaysAllow: return true
            case .default:
                if allowDefaultReadOnce { allowDefaultReadOnce = false; return true }
                return false
            default: return false
            }
        }
        return true
    }

    private func pngData(from pb: NSPasteboard) -> Data? {
        if let png = pb.data(forType: .png) { return png }
        guard let tiff = pb.data(forType: .tiff), tiff.count <= Constants.clipboardMaxImageBytes,
              let rep = NSBitmapImageRep(data: tiff) else { return nil }
        return rep.representation(using: .png, properties: [:])
    }
}
```

- [ ] **Step 4: 테스트 통과** — `ClipboardMonitorTests` 7개, 총 48개.

- [ ] **Step 5: 커밋** — `feat: ClipboardMonitor — 폴링·프라이버시 게이트·제외 타입·분류 + 테스트`

---

### Task 3: ClipboardView + 루트 유휴 리셋

**Files:**
- Create: `Sources/Clipboard/ClipboardView.swift`
- Modify: `Sources/NotchWindow/NotchRootView.swift`, `Sources/App/Preferences.swift`

**Interfaces:**
- Produces: `struct ClipboardView: View { let store: ClipboardStore; let monitor: ClipboardMonitor }`; `Notification.Name.openNotchClearClipboard`
- 루트 유휴 리셋(P2 리뷰 R3): `NotchRootView`의 펼친 콘텐츠에 `.simultaneousGesture(TapGesture().onEnded { viewModel.resetIdle() })`와 `.onContinuousHover { _ in viewModel.resetIdle() }`(호버 리셋은 `hoverToOpen`과 무관, 펼친 상태에서 마우스가 움직이면 유휴 연장) — 각 모듈의 개별 `resetIdle()` 호출은 그대로 두어도 무해.

- [ ] **Step 1: Preferences에 알림 이름**

`Sources/App/Preferences.swift` 끝에:
```swift
extension Notification.Name {
    /// 설정 창의 "Clear history" → 스토어가 비운다.
    static let openNotchClearClipboard = Notification.Name("com.holyshine11.opennotch.clearClipboard")
}
```

- [ ] **Step 2: ClipboardView**

`Sources/Clipboard/ClipboardView.swift`:
```swift
import AppKit
import SwiftUI

/// 하단 44pt 행: 칩 5개 + ▸(전체 목록) + 검색. 펼치면 위쪽 두 칸을 덮는 overlay 목록(패널 크기 불변).
struct ClipboardView: View {
    let store: ClipboardStore
    let monitor: ClipboardMonitor
    @Environment(\.notchHost) private var host
    @State private var expanded = false
    @State private var query = ""
    @FocusState private var searchFocused: Bool

    var body: some View {
        HStack(spacing: 6) {
            if store.pasteboardBlocked {
                blockedNotice
            } else if !monitor.isEnabled {
                Button("Enable clipboard history") { monitor.enableWithUserAction() }.buttonStyle(.bordered)
                Spacer()
            } else {
                chips
                Button { expanded.toggle() } label: { Image(systemName: expanded ? "chevron.down" : "chevron.right") }
                    .buttonStyle(.borderless).help("Show all")
                TextField("Search", text: $query)
                    .textFieldStyle(.roundedBorder).frame(width: 120)
                    .focused($searchFocused)
                    .onChange(of: searchFocused) { _, focused in
                        host?.setWantsKey(focused)
                        if focused { expanded = true }
                    }
            }
        }
        .frame(maxWidth: .infinity)
        .padding(.horizontal, 8)
        .background(Color.white.opacity(0.05), in: RoundedRectangle(cornerRadius: 10))
        .overlay(alignment: .bottom) {
            if expanded {
                fullList
                    .frame(height: Constants.clipboardListHeight)
                    .background(Color.black.opacity(0.92), in: RoundedRectangle(cornerRadius: 10))
                    .offset(y: -(Constants.clipboardListHeight + 8))   // 행 위로 올려 상단 두 칸을 덮는다
                    .transition(.opacity)
            }
        }
        .animation(.easeInOut(duration: 0.15), value: expanded)
    }

    private var chips: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 6) {
                ForEach(store.items.prefix(Constants.clipboardChipCount)) { item in
                    chip(item)
                }
                if store.items.isEmpty {
                    Text("Copy something to see it here").font(.caption).foregroundStyle(.secondary)
                }
            }
        }
    }

    private func chip(_ item: ClipItem) -> some View {
        Button { copy(item) } label: {
            Label(item.title, systemImage: icon(for: item.kind))
                .font(.caption).lineLimit(1)
                .padding(.horizontal, 8).padding(.vertical, 4)
                .background(Color.white.opacity(0.1), in: Capsule())
        }
        .buttonStyle(.plain)
        .contextMenu { Button("Delete", role: .destructive) { store.remove(id: item.id) } }
    }

    private var fullList: some View {
        ScrollView {
            LazyVStack(alignment: .leading, spacing: 2) {
                ForEach(store.search(query)) { item in
                    Button { copy(item) } label: {
                        HStack(spacing: 6) {
                            Image(systemName: icon(for: item.kind)).frame(width: 14)
                            Text(item.title).lineLimit(1)
                            Spacer()
                            Text(item.createdAt, style: .time).foregroundStyle(.secondary)
                        }
                        .font(.caption)
                        .padding(.horizontal, 8).padding(.vertical, 4)
                        .contentShape(Rectangle())
                    }
                    .buttonStyle(.plain)
                    .contextMenu { Button("Delete", role: .destructive) { store.remove(id: item.id) } }
                }
            }
            .padding(4)
        }
    }

    private var blockedNotice: some View {
        HStack(spacing: 8) {
            Text("Allow OpenNotch to read the clipboard in System Settings").font(.caption)
            Button("Open Settings") {
                if let url = URL(string: Constants.pasteboardPrivacySettingsURL) { NSWorkspace.shared.open(url) }
            }
            .buttonStyle(.link).font(.caption)
        }
    }

    private func copy(_ item: ClipItem) {
        store.write(item)
        host?.showToast(String(localized: "Copied — press ⌘V to paste"), action: nil)
        host?.resetIdle()
    }

    private func icon(for kind: ClipKind) -> String {
        switch kind {
        case .text: "text.alignleft"
        case .url: "link"
        case .image: "photo"
        case .files: "doc"
        }
    }
}
```
`Constants.swift`에 추가(설정 URL은 `x-apple.systempreferences:` 스킴이라 `https://` 규칙 대상 아님):
```swift
    static let pasteboardPrivacySettingsURL: String = "x-apple.systempreferences:com.apple.preference.security?Privacy_Pasteboard"
```

- [ ] **Step 3: 루트 유휴 리셋**

`NotchRootView.swift`의 `content` 계산 프로퍼티(펼친 콘텐츠) 마지막에 추가:
```swift
        .simultaneousGesture(TapGesture().onEnded { viewModel.resetIdle() })
        .onContinuousHover { phase in if case .active = phase { viewModel.resetIdle() } }
```

- [ ] **Step 4: 빌드** — rc=0(테스트 48 유지). 아직 페인이 연결되지 않아 화면에는 안 보인다.

- [ ] **Step 5: 커밋** — `feat: ClipboardView(칩·overlay 목록·검색·삭제·게이트 안내) + 루트 유휴 리셋`

---

### Task 4: 배선 — AppDelegate·설정·잠금·상한

**Files:**
- Modify: `Sources/App/AppDelegate.swift`, `Sources/App/SettingsView.swift`

- [ ] **Step 1: AppDelegate**

프로퍼티 추가:
```swift
    private var clipboardStore: ClipboardStore?
    private var clipboardMonitor: ClipboardMonitor?
```
`applicationDidFinishLaunching`에서 셸프 배선 뒤(`controller.show()` 앞)에:
```swift
        let clipboard = ClipboardStore()
        let monitor = ClipboardMonitor(store: clipboard)
        clipboardStore = clipboard
        clipboardMonitor = monitor
        monitor.start()
        controller.paneProvider = { (media: nil, shelf: AnyView(ShelfView(store: shelf)), clipboard: AnyView(ClipboardView(store: clipboard, monitor: monitor))) }
        observers.append(NotificationCenter.default.addObserver(forName: .openNotchClearClipboard, object: nil, queue: .main) { [weak clipboard] _ in
            MainActor.assumeIsolated { clipboard?.removeAll() }
        })
```
(기존 `controller.paneProvider = { (media: nil, shelf: ..., clipboard: nil) }` 줄은 위 줄로 교체.)
`applyPreferences(to:)` 끝에:
```swift
        if let clipboardMonitor, let clipboardStore {
            let enabled = defaults.bool(forKey: PrefKey.clipboardEnabled)
            if clipboardMonitor.isEnabled != enabled { clipboardMonitor.isEnabled = enabled }
            let limit = defaults.integer(forKey: PrefKey.clipboardLimit)
            if limit > 0, clipboardStore.limit != limit { clipboardStore.limit = limit }
        }
```
그리고 `applicationWillTerminate(_:)` 추가: `clipboardMonitor?.stop()`.

- [ ] **Step 2: 설정 창 Clear 버튼**

`SettingsView.swift`의 `Section("Clipboard")` 안, Stepper 아래:
```swift
                Button("Clear history") { NotificationCenter.default.post(name: .openNotchClearClipboard, object: nil) }
```

- [ ] **Step 3: 빌드·테스트·실기 확인**

rc=0, 48개. 앱 실행 → 텍스트를 복사(⌘C)하면 1초 안에 칩 등장 → 칩 클릭 → 토스트 "Copied — press ⌘V to paste" → 다른 앱에서 ⌘V로 붙여넣기 됨 → 앱이 쓴 항목은 다시 캡처되지 않음(칩 개수 불변). 이미지 복사(스크린샷 ⌃⇧⌘4) → "Image" 칩. Finder에서 파일 ⌘C → "x.txt" 칩. 1Password 등 비밀번호 복사 → 캡처 안 됨. ▸ → 목록 overlay가 위 두 칸 위에 뜸, 검색 입력 중 유휴 접힘 없음, Esc → 패널 접힘. 설정 "Keep clipboard history" OFF → 캡처 중단, "Clear history" → 비워짐. 개발자 플래그 `defaults write com.holyshine11.opennotch EnablePasteboardPrivacyDeveloperPreview -bool yes`(컨테이너 prefs에!) 후 재실행 → 칩 대신 "Allow OpenNotch…" 안내, 플래그 삭제.

- [ ] **Step 4: 커밋** — `feat: 클립보드 페인 배선, 설정 반영(사용·상한·Clear), 화면 잠금 일시정지`

---

## P3 완료 기준
- 테스트 48개 통과(ClipboardStoreTests 8, ClipboardMonitorTests 7 추가).
- 복사 → 칩 → 클릭 복사 → ⌘V 동작, 제외 타입 미캡처, 이미지/파일 분류, 검색·삭제·Clear, 프라이버시 게이트 안내.
- 다음 계획: P4 미디어(`paneProvider.media`, `#if MEDIA_ENABLED`, entitlements 2키 추가 + `EntitlementsTests` 갱신, NoMedia 빌드 구성).
